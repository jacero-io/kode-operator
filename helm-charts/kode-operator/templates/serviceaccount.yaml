apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kode-operator.fullname" . }}-controller-manager
  labels:
  {{- include "kode-operator.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.controllerManager.serviceAccount.annotations | nindent 4 }}