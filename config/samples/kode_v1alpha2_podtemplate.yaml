apiVersion: kode.jacero.io/v1alpha2
kind: PodTemplate
metadata:
  labels:
    app.kubernetes.io/name: kode-operator
    app.kubernetes.io/managed-by: kustomize
  name: podtemplate-sample
spec:
  # TODO(user): Add fields here
