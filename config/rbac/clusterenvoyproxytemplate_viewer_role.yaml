# permissions for end users to view clusterenvoyproxytemplates.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kode-operator
    app.kubernetes.io/managed-by: kustomize
  name: clusterenvoyproxytemplate-viewer-role
rules:
- apiGroups:
  - kode.jacero.io
  resources:
  - clusterenvoyproxytemplates
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kode.jacero.io
  resources:
  - clusterenvoyproxytemplates/status
  verbs:
  - get
