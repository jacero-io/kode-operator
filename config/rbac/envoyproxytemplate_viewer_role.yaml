# permissions for end users to view envoyproxytemplates.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kode-operator
    app.kubernetes.io/managed-by: kustomize
  name: envoyproxytemplate-viewer-role
rules:
- apiGroups:
  - kode.jacero.io
  resources:
  - envoyproxytemplates
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kode.jacero.io
  resources:
  - envoyproxytemplates/status
  verbs:
  - get
