# permissions for end users to view clusterkodetemplates.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kode-operator
    app.kubernetes.io/managed-by: kustomize
  name: clusterkodetemplate-viewer-role
rules:
- apiGroups:
  - kode.jacero.io
  resources:
  - clusterkodetemplates
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kode.jacero.io
  resources:
  - clusterkodetemplates/status
  verbs:
  - get
