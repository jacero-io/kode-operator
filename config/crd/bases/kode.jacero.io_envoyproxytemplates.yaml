---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: envoyproxytemplates.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: EnvoyProxyTemplate
    listKind: EnvoyProxyTemplateList
    plural: envoyproxytemplates
    singular: envoyproxytemplate
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EnvoyProxyTemplate is the Schema for the envoyproxytemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EnvoyProxyTemplateSpec defines the desired state of EnvoyProxyTemplate
            properties:
              httpFilters:
                description: HTTPFilters is a list of Envoy HTTP filters to be applied
                items:
                  description: HTTPFilter represents an individual HTTP filter configuration
                  properties:
                    name:
                      description: Name is the name of the HTTP filter
                      minLength: 1
                      type: string
                    typed_config:
                      description: |-
                        TypedConfig is the typed configuration for the HTTP filter
                        It is intentionally the same as the Envoy filter's typed_config field to make it easier to copy-paste
                      properties:
                        '@type':
                          description: Type is the type of the configuration
                          minLength: 1
                          type: string
                        config:
                          description: Config is the configuration for the filter
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - '@type'
                      type: object
                  required:
                  - name
                  - typed_config
                  type: object
                minItems: 1
                type: array
              image:
                default: envoyproxy/envoy:v1.30-latest
                description: Image is the Docker image for the Envoy proxy
                minLength: 1
                type: string
            required:
            - httpFilters
            - image
            type: object
          status:
            description: EnvoyProxyTemplateStatus defines the observed state of EnvoyProxyTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
