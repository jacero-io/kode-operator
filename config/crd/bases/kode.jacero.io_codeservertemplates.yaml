---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: codeservertemplates.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: CodeServerTemplate
    listKind: CodeServerTemplateList
    plural: codeservertemplates
    singular: codeservertemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CodeServerTemplate is the Schema for the codeservertemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CodeServerTemplateSpec defines the desired state of CodeServerTemplate
            properties:
              args:
                description: Specifies the envs
                items:
                  type: string
                type: array
              configPath:
                default: /config
                description: ConfigPath is the path to the config directory for code-server
                minLength: 1
                type: string
              defaultWorkspace:
                description: DefaultWorkspace is the default workspace directory for
                  code-server (eg. /config/workspace)
                minLength: 1
                type: string
              envoyProxyTemplateRef:
                description: EnvoyProxyRef is the reference to the EnvoyProxy configuration
                properties:
                  kind:
                    description: Kind is the kind of the reference (for EnvoyProxyTemplate)
                    type: string
                  name:
                    description: Name is the name of the EnvoyProxyTemplate
                    type: string
                required:
                - kind
                - name
                type: object
              envs:
                description: Specifies the envs
                items:
                  type: string
                type: array
              hashedPassword:
                description: HashedPassword is the hashed password for code-server
                type: string
              image:
                default: lscr.io/linuxserver/code-server:latest
                description: Image is the container image for code-server
                minLength: 1
                type: string
              password:
                description: Password is the password for code-server
                minLength: 1
                type: string
              pgid:
                default: 1000
                description: PGID is the group ID for the code-server process
                format: int64
                type: integer
              port:
                default: 3000
                description: Port is the port for the code-server process. Used by
                  EnvoyProxy to expose the service.
                format: int32
                minimum: 1
                type: integer
              proxyDomain:
                description: ProxyDomain specifies the domain to proxy for code-server
                minLength: 1
                type: string
              puid:
                default: 1000
                description: PUID is the user ID for the code-server process
                format: int64
                type: integer
              sudoPassword:
                description: SudoPassword is the sudo password for code-server
                type: string
              sudoPasswordHash:
                description: SudoPasswordHash is the hashed sudo password for code-server
                type: string
              tz:
                default: Europe/Stockholm
                description: TZ is the timezone for the code-server process
                type: string
            required:
            - image
            - password
            type: object
          status:
            description: CodeServerTemplateStatus defines the observed state of CodeServerTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
