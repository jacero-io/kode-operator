---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clusterenvoyproxytemplates.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: ClusterEnvoyProxyTemplate
    listKind: ClusterEnvoyProxyTemplateList
    plural: clusterenvoyproxytemplates
    singular: clusterenvoyproxytemplate
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterEnvoyProxyTemplate is the Schema for the clusterenvoyproxytemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterEnvoyProxyTemplateSpec defines the desired state of
              ClusterEnvoyProxyTemplate
            properties:
              clusters:
                description: Clusters is a list of Envoy clusters
                items:
                  properties:
                    connect_timeout:
                      description: ConnectTimeout is the timeout for connecting to
                        the cluster
                      type: string
                    lb_policy:
                      description: LbPolicy is the load balancing policy for the cluster
                      type: string
                    name:
                      description: Name is the name of the cluster
                      minLength: 1
                      type: string
                    type:
                      description: Type is the type of the cluster
                      type: string
                    typed_extension_protocol_options:
                      description: TypedExtensionProtocolOptions is a map of typed
                        extension protocol options
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - connect_timeout
                  - lb_policy
                  - name
                  - type
                  type: object
                type: array
              filters:
                description: HTTPFilters is a list of Envoy HTTP filters to be applied
                items:
                  description: HTTPFilter represents an individual HTTP filter configuration
                  properties:
                    name:
                      description: Name is the name of the HTTP filter
                      minLength: 1
                      type: string
                    typed_config:
                      description: |-
                        TypedConfig is the typed configuration for the HTTP filter
                        It is intentionally the same as the Envoy filter's typed_config field to make it easier to copy-paste
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  - typed_config
                  type: object
                type: array
              image:
                default: envoyproxy/envoy:v1.30-latest
                description: Image is the Docker image for the Envoy proxy
                minLength: 1
                type: string
            required:
            - filters
            - image
            type: object
          status:
            description: ClusterEnvoyProxyTemplateStatus defines the observed state
              of ClusterEnvoyProxyTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
