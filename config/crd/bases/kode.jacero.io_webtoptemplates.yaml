---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: webtoptemplates.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: WebtopTemplate
    listKind: WebtopTemplateList
    plural: webtoptemplates
    singular: webtoptemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WebtopTemplate is the Schema for the webtoptemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WebtopTemplateSpec defines the desired state of WebtopTemplate
            properties:
              args:
                description: Specifies the args
                items:
                  type: string
                type: array
              customUser:
                default: abc
                description: CustomUser HTTP Basic auth username, abc is default.
                type: string
              disableIpv6:
                description: DisableIpv6 If set to true or any value this will disable
                  IPv6
                type: boolean
              drinode:
                description: Drinode If mounting in /dev/dri for DRI3 GPU Acceleration
                  allows you to specify the device to use IE /dev/dri/renderD128
                type: string
              envoyProxyTemplateRef:
                description: EnvoyProxyRef is the reference to the EnvoyProxy configuration
                properties:
                  kind:
                    description: Kind is the kind of the reference (for EnvoyProxyTemplate)
                    type: string
                  name:
                    description: Name is the name of the EnvoyProxyTemplate
                    type: string
                required:
                - kind
                - name
                type: object
              envs:
                description: Specifies the envs
                items:
                  type: string
                type: array
              fmHome:
                default: /config
                description: FmHome This is the home directory (landing) for the file
                  manager, default "/config".
                type: string
              image:
                default: lscr.io/linuxserver/webtop:latest
                description: Image is the container image for webtop
                minLength: 1
                type: string
              lcAll:
                description: LcAll Set the Language for the container to run as IE
                  fr_FR.UTF-8 ar_AE.UTF-8
                type: string
              noDecor:
                description: NoDecor If set the application will run without window
                  borders in openbox for use as a PWA.
                type: boolean
              noFull:
                description: NoFull Do not autmatically fullscreen applications when
                  using openbox.
                type: boolean
              password:
                description: Password HTTP Basic auth password, abc is default. If
                  unset there will be no auth
                type: string
              pgid:
                default: 1000
                description: PGID is the group ID for the code-server process
                format: int64
                type: integer
              port:
                default: 3000
                description: Port is the port for the webtop process. Used by EnvoyProxy
                  to expose the service.
                format: int32
                minimum: 1
                type: integer
              puid:
                default: 1000
                description: PUID is the user ID for the code-server process
                format: int64
                type: integer
              startDocker:
                default: true
                description: StartDocker If set to false a container with privilege
                  will not automatically start the DinD Docker setup.
                type: boolean
              subfolder:
                description: Subfolder Subfolder for the application if running a
                  subfolder reverse proxy, need both slashes IE /subfolder/
                type: string
              title:
                default: KasmVNC Client
                description: Title The page title displayed on the web browser, default
                  "KasmVNC Client".
                type: string
              tz:
                default: Europe/Stockholm
                description: TZ is the timezone for the code-server process
                type: string
            required:
            - image
            type: object
          status:
            description: WebtopTemplateStatus defines the observed state of WebtopTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
