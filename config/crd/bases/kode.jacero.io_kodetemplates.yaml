---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: kodetemplates.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: KodeTemplate
    listKind: KodeTemplateList
    plural: kodetemplates
    singular: kodetemplate
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KodeTemplate is the Schema for the kodetemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KodeTemplateSpec defines the desired state of KodeTemplate
            properties:
              args:
                description: Specifies the envs
                items:
                  type: string
                type: array
              configPath:
                default: /config
                description: ConfigPath is the path to the config directory for code-server
                minLength: 1
                type: string
              defaultWorkspace:
                description: DefaultWorkspace is the default workspace directory for
                  code-server (eg. /config/workspace)
                minLength: 1
                type: string
              envoyProxyTemplateRef:
                description: EnvoyProxyRef is the reference to the EnvoyProxy configuration
                properties:
                  kind:
                    description: Kind is the kind of the reference (for EnvoyProxyTemplate)
                    type: string
                  name:
                    description: Name is the name of the EnvoyProxyTemplate
                    type: string
                required:
                - kind
                - name
                type: object
              envs:
                description: Specifies the envs
                items:
                  type: string
                type: array
              hashedPassword:
                description: HashedPassword is the hashed password for code-server
                type: string
              image:
                default: lscr.io/linuxserver/code-server:latest
                description: Image is the Docker image for code-server
                minLength: 1
                type: string
              password:
                description: Password is the password for code-server
                minLength: 1
                type: string
              pgid:
                default: 1000
                description: PGID is the group ID for the code-server process
                format: int64
                type: integer
              puid:
                default: 1000
                description: PUID is the user ID for the code-server process
                format: int64
                type: integer
              servicePort:
                default: 8443
                description: ServicePort is the port for the code-server service
                format: int32
                minimum: 1
                type: integer
              sudoPassword:
                description: SudoPassword is the sudo password for code-server
                type: string
              sudoPasswordHash:
                description: SudoPasswordHash is the hashed sudo password for code-server
                type: string
              tz:
                default: Europe/Stockholm
                description: TZ is the timezone for the code-server process
                type: string
              url:
                description: URL specifies the url for used to access the code-server
                type: string
            required:
            - envoyProxyTemplateRef
            - image
            - password
            type: object
          status:
            description: KodeTemplateStatus defines the observed state of KodeTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
