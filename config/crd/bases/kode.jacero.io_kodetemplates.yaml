---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: kodetemplates.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: KodeTemplate
    listKind: KodeTemplateList
    plural: kodetemplates
    singular: kodetemplate
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KodeTemplate is the Schema for the kodetemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KodeTemplateSpec defines the desired state of KodeTemplate
            properties:
              args:
                description: Specifies the args
                items:
                  type: string
                type: array
              defaultWorkspace:
                default: workspace
                description: DefaultWorkspace is the default workspace directory.
                  Defaults to 'workspace'.
                minLength: 3
                pattern: ^[^/].*$
                type: string
              envoyProxyTemplateRef:
                description: EnvoyProxyTemplateRef is the reference to the EnvoyProxy
                  configuration
                properties:
                  kind:
                    description: Kind is the kind of the reference (for EnvoyProxyTemplate)
                    type: string
                  name:
                    description: Name is the name of the EnvoyProxyTemplate
                    type: string
                required:
                - kind
                - name
                type: object
              envs:
                description: Specifies the envs
                items:
                  type: string
                type: array
              home:
                default: /config
                description: Home is the path to the directory for the user data
                minLength: 3
                type: string
              image:
                description: Image is the container image for the service
                minLength: 1
                type: string
              pgid:
                default: 1000
                description: PGID is the group ID for the service process
                format: int64
                type: integer
              port:
                default: 3000
                description: Port is the port for the service process. Used by EnvoyProxy
                  to expose the service.
                format: int32
                minimum: 1
                type: integer
              puid:
                default: 1000
                description: PUID is the user ID for the service process
                format: int64
                type: integer
              tz:
                default: UTC
                description: TZ is the timezone for the service process
                type: string
            required:
            - image
            type: object
          status:
            description: KodeTemplateStatus defines the observed state of KodeTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
