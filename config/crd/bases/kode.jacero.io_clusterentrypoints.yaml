---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: clusterentrypoints.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: ClusterEntryPoint
    listKind: ClusterEntryPointList
    plural: clusterentrypoints
    singular: clusterentrypoint
  scope: Cluster
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: ClusterEntryPoint is the Schema for the clusterentrypoints API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterEntryPointSpec defines the desired state of ClusterEntryPoint
            properties:
              gatewaySpec:
                description: GatewaySpec defines the GatewaySpec for the EntryPoint.
                  Only one of GatewaySpec or IngressSpec can be set.
                properties:
                  gatewayClassName:
                    description: GatewayClassName is the name of the GatewayClass
                      resource.
                    type: string
                  patchPolicy:
                    description: PatchPolicy defines the custom patch policy for the
                      Envoy Proxy config generated by Envoy Gateway. For experienced
                      users.
                    properties:
                      clusters:
                        description: Clusters is a list of Envoy clusters
                        items:
                          properties:
                            connect_timeout:
                              description: ConnectTimeout is the timeout for connecting
                                to the cluster
                              type: string
                            lb_policy:
                              description: LbPolicy is the load balancing policy for
                                the cluster
                              type: string
                            load_assignment:
                              description: LoadAssignment is the load assignment for
                                the cluster
                              properties:
                                cluster_name:
                                  description: ClusterName is the name of the cluster
                                  minLength: 1
                                  type: string
                                endpoints:
                                  description: Endpoints is a list of endpoints
                                  items:
                                    properties:
                                      lb_endpoints:
                                        description: LbEndpoints is the load balancer
                                          endpoints
                                        items:
                                          properties:
                                            endpoint:
                                              description: Endpoints is a list of
                                                endpoints
                                              properties:
                                                address:
                                                  description: Address is the address
                                                    of the load balancer endpoint
                                                  properties:
                                                    socket_address:
                                                      description: SocketAddress is
                                                        the socket address
                                                      properties:
                                                        address:
                                                          description: Address is
                                                            the address of the socket
                                                          type: string
                                                        port_value:
                                                          description: PortValue is
                                                            the port of the socket
                                                          type: integer
                                                      required:
                                                      - address
                                                      - port_value
                                                      type: object
                                                  required:
                                                  - socket_address
                                                  type: object
                                              required:
                                              - address
                                              type: object
                                          required:
                                          - endpoint
                                          type: object
                                        type: array
                                    required:
                                    - lb_endpoints
                                    type: object
                                  type: array
                              required:
                              - cluster_name
                              - endpoints
                              type: object
                            name:
                              description: Name is the name of the cluster
                              minLength: 1
                              type: string
                            type:
                              description: Type is the type of the cluster
                              type: string
                            typed_extension_protocol_options:
                              description: TypedExtensionProtocolOptions is a map
                                of typed extension protocol options
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - connect_timeout
                          - lb_policy
                          - load_assignment
                          - name
                          - type
                          type: object
                        type: array
                      httpFilters:
                        description: HTTPFilters is a list of Envoy HTTP filters to
                          be applied
                        items:
                          description: HTTPFilter represents an individual HTTP filter
                            configuration
                          properties:
                            name:
                              description: Name is the name of the HTTP filter
                              minLength: 1
                              type: string
                            typed_config:
                              description: |-
                                TypedConfig is the typed configuration for the HTTP filter
                                It is intentionally the same as the Envoy filter's typed_config field to make it easier to copy-paste
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - name
                          - typed_config
                          type: object
                        type: array
                    type: object
                type: object
              routingType:
                description: Type is the way the Kode resource is accessed. It could
                  be subdomain or path.
                enum:
                - domain
                - path
                type: string
              url:
                description: |-
                  URL is the domain name to use either as a suffix in the case of Type=domain or as a prefix/domain in the case of Type=path.
                  When the type is domain, the controller will try to publish the Kode resource as a subdomain of the given domain (e.g <kode-resource>.kode.example.com).
                  When the type is path, the controller will try to publish the Kode resource as a path of the given URL (e.g kode.example.com/<kode-resource>).
                type: string
            type: object
          status:
            description: ClusterEntryPointStatus defines the observed state of ClusterEntryPoint
            properties:
              conditions:
                description: Conditions reflect the current state of the template
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastError:
                description: LastError contains the last error message encountered
                  during reconciliation.
                type: string
              lastErrorTime:
                description: LastErrorTime is the timestamp when the last error occurred.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation of
                  the Kode resource.
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the EntryPointPhase
                  resource.
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
