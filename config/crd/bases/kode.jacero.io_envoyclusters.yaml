---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: envoyclusters.kode.jacero.io
spec:
  group: kode.jacero.io
  names:
    kind: EnvoyCluster
    listKind: EnvoyClusterList
    plural: envoyclusters
    singular: envoycluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EnvoyCluster is the Schema for the envoyclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EnvoyClusterSpec defines the desired state of EnvoyCluster
            properties:
              connectTimeout:
                description: ConnectTimeout specifies the timeout for establishing
                  new connections
                pattern: ^([0-9]+(s|ms|us|ns))$
                type: string
              loadAssignment:
                description: LoadAssignment specifies the cluster load assignment
                properties:
                  clusterName:
                    description: ClusterName is the name of the cluster load assignment
                    minLength: 1
                    type: string
                  endpoints:
                    description: Endpoints is a list of endpoints for the cluster
                    items:
                      description: Endpoint specifies a single endpoint configuration
                      properties:
                        address:
                          description: Address specifies the address of the endpoint
                          type: string
                        port:
                          description: Port specifies the port of the endpoint
                          format: int32
                          type: integer
                      required:
                      - address
                      - port
                      type: object
                    minItems: 1
                    type: array
                required:
                - clusterName
                - endpoints
                type: object
              name:
                description: Name of the cluster
                minLength: 1
                type: string
              type:
                description: Type specifies the type of the cluster (e.g., STATIC,
                  STRICT_DNS)
                enum:
                - STATIC
                - STRICT_DNS
                - LOGICAL_DNS
                - EDS
                - ORIGINAL_DST
                - RELOADABLE
                type: string
            required:
            - connectTimeout
            - loadAssignment
            - name
            - type
            type: object
          status:
            description: EnvoyClusterStatus defines the observed state of EnvoyCluster
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
